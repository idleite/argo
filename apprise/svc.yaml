apiVersion: v1
kind: Service
metadata:
  name: mailrise
  namespace: mailrise
  labels:
    app.kubernetes.io/name: mailrise

spec:
  type: ClusterIP
  ports:
    - port: 8025
      targetPort: smtp
      protocol: TCP
      name: smtp
  selector:
    app.kubernetes.io/name: mailrise
    app: mailrise